package com.sample.test;


public class TestShiroConfiguration {

	public static void main(String[] args) {
	/*
		Realm realm = null;//instantiate or acquire a Realm instance.  We'll discuss Realms later.

				DefaultSecurityManager securityManager = new DefaultSecurityManager(realm);

				//Make the SecurityManager instance available to the entire application via static memory:
				SecurityUtils.setSecurityManager(securityManager);

				SessionDAO session = new MemorySessionDAO();
				
				((DefaultSessionManager)securityManager.getSessionManager()).setSessionDAO(session);

	*/
	}

}
